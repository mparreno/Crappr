<ul class="star-rating">
	<% (1..5).each do |i| %>
		<li>
			<%= link_to i, rate_toilet_path, 
				{:class => "stars-#{i} #{ "current-rating" if i == toilet.rating }", 
				 :title => "#{i} stars out of 5", 
				 "data-rating" => i } %>
		</li>
	<% end %>
</ul>
(<%= toilet.ratings.count %> ratings)